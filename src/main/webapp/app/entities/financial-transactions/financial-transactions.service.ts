import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { type IFinancialTransactions } from '@/shared/model/financial-transactions.model';

const baseApiUrl = 'api/financial-transactions';

export default class FinancialTransactionsService {
  public find(id: number): Promise<IFinancialTransactions> {
    return new Promise<IFinancialTransactions>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl + `?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: IFinancialTransactions): Promise<IFinancialTransactions> {
    return new Promise<IFinancialTransactions>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: IFinancialTransactions): Promise<IFinancialTransactions> {
    return new Promise<IFinancialTransactions>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public partialUpdate(entity: IFinancialTransactions): Promise<IFinancialTransactions> {
    return new Promise<IFinancialTransactions>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
